use wasm_bindgen::JsValue;
use wasm_bindgen::prelude::*;

use web_sys::{HtmlCanvasElement, WebGlShader, WebGlProgram, WebGlRenderingContext};

pub fn _draw(_canvas: &HtmlCanvasElement) -> Result<(), JsValue> {

    Ok(())
}
